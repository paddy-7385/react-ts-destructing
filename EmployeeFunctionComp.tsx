import React from 'react';

export default function Employee({firstName,lastName}){
     return(
      <div>
        <div>Employee firstName: {firstName}</div>
        <div>Employee lastName: {lastName}</div>
      </div>
    )
}
